# 不動産ポータル - 実装状況レポート

**更新日**: 2025-12-17
**ステータス**: Phase 0-3 実装完了、Phase 4-6 部分実装

---

## ✅ 実装完了機能

### Phase 0: PoC検証機能

#### ✅ スクレイピング安定性
- **実装場所**: `backend/internal/scraper/scraper.go`
- **機能**:
  - レート制限（900ms + ランダムジッター）
  - サーキットブレーカー（WAFブロック検出）
  - Exponential backoffリトライ
  - 同時実行数制限（デフォルト: 2）
  - グローバルレート制限
- **テストスクリプト**: `backend/cmd/test-poc/main.go`
- **実行方法**: `POC-TEST-GUIDE.md` 参照

#### ✅ 検索機能（Meilisearch）
- **実装場所**: `backend/internal/search/meilisearch.go`
- **機能**:
  - 全文検索（タイトル、住所、駅名）
  - フィルタリング（家賃、間取り、徒歩時間、面積、築年数、階数）
  - ソート（新着順、家賃順、駅近順、築年数順、面積順）
  - ファセット集計
- **エンドポイント**:
  - `GET /api/properties` - 全物件取得
  - `GET /api/search?q=新宿` - キーワード検索
  - `POST /api/search/advanced` - 高度な検索

#### ✅ 画像外部参照
- **実装場所**: `backend/internal/scraper/scraper.go:546-577`
- **機能**:
  - HTTP HEADで画像URL検証
  - 200 OK以外は NULL に設定
  - フロントエンドでプレースホルダにフォールバック
- **画像URL**: Yahoo不動産の外部画像URL（yimg.jp）

#### ✅ Yahoo不動産リンク
- **実装場所**:
  - Backend: Property.DetailURL
  - Frontend: `frontend-next/src/app/page.tsx:1116-1119`
- **機能**: `target="_blank" rel="noreferrer"` で遷移

---

### Phase 1: 技術スタック

#### ✅ Database: PostgreSQL + MySQL 8.x 両対応
- **PostgreSQL実装**: `backend/internal/database/postgres.go`
- **MySQL実装**: `backend/internal/database/mysql.go` (GORM)
- **環境変数**: `DB_TYPE=mysql` または `DB_TYPE=postgres`
- **マイグレーション**: GORM AutoMigrate（既存データ保持）

#### ✅ Frontend: Next.js 14
- **実装場所**: `frontend-next/`
- **主要機能**:
  - App Router
  - localStorage永続化（候補・微妙・除外）
  - 非表示機能
  - メモ機能
  - レスポンシブデザイン
- **URL**: http://localhost:5176

#### ✅ ORM: GORM
- **実装場所**: `backend/internal/database/mysql.go`
- **models**: `backend/internal/models/property.go`
- **機能**:
  - Upsert（detail_urlでの重複回避）
  - 論理削除（status, removed_at）
  - LastSeenAt（最終確認日時）

---

### Phase 2: データ品質改善

#### ✅ URL正規化機能
- **実装場所**:
  - `backend/internal/scraper/scraper.go:505-522` (normalizeURL)
  - `backend/internal/database/mysql.go:220-238` (normalizeURL)
- **機能**:
  - クエリ文字列削除
  - 末尾スラッシュ削除
  - HTTPSに統一
  - Canonical URL優先採用

#### ✅ 画像URL生存確認
- **実装場所**: `backend/internal/scraper/scraper.go:546-577`
- **機能**:
  - HTTP HEADで200 OKチェック
  - 失敗時はNULL化
  - タイムアウト: 5秒

#### ✅ バリデーション
- **実装場所**: `backend/internal/scraper/scraper.go`
- **対象**:
  - Rent: 1万円〜1000万円
  - Area: 5〜500㎡
  - WalkTime: 1〜60分
  - BuildingAge: 0〜100年
  - Floor: 0〜100階

#### ✅ Upsert機能
- **PostgreSQL**: `ON CONFLICT (detail_url) DO UPDATE`
- **MySQL/GORM**: detail_url検索 → Create/Update
- **動作**: `created_at`は初回固定、`fetched_at`は更新

---

### Phase 3: 差分検出・毎日更新機能

#### ✅ テーブル設計
- **PropertySnapshot**: `backend/internal/models/snapshot.go`
  - 毎日のスナップショット記録
  - 変更検出（rent, status, area, floor_plan等）
- **PropertyChange**: 変更履歴
  - 変更タイプ（rent_changed, status_changed等）
  - 変更量（ChangeMagnitude）

#### ✅ 差分検出ロジック
- **実装場所**:
  - `backend/internal/snapshot/snapshot.go:56-184`
  - `backend/internal/database/mysql.go:157-218`
- **機能**:
  - 前日との比較
  - 新規物件検出
  - 更新物件検出
  - 消滅物件検出（論理削除）
  - 変更履歴の記録

#### ✅ スケジューラー
- **実装場所**: `backend/internal/scheduler/scheduler.go`
- **機能**:
  - Cron形式（HH:MM指定）
  - 手動実行API: `POST /api/admin/scheduler/run`
  - エラー時の自動停止
  - 変更検出とスナップショット作成
- **設定**: `backend/config/scraper_config.yaml`
  - `daily_run_enabled: true/false`
  - `daily_run_time: "02:00"`

---

## 🟡 部分実装機能

### Phase 4: 負荷最小化・安全運用設計

#### ✅ 設定外部化
- **実装**: `backend/config/scraper_config.yaml`
- **読み込み**: `backend/internal/config/config.go`

#### ✅ レート制限
- **実装**: `backend/internal/ratelimit/`
- **機能**:
  - リクエスト間隔制限
  - 1分/1時間/1日あたりの制限
  - サーキットブレーカー

---

## ⚠️ 未実装 / 今後の実装予定

### Phase 4.5: 物理削除バッチ

- [ ] delete_logs テーブル
- [ ] 90日経過物件の物理削除
- [ ] dry-runモード
- [ ] 週1回実行スケジュール

### Phase 5: アーキテクチャ改善

- [ ] Fetcher Interface
- [ ] AffiliateFeed実装
- [ ] DI構造（wire）

### Phase 6: 管理機能

- [ ] 管理画面API（統計情報）
- [ ] 管理画面UI（Next.js）
- [ ] 認証（Basic Auth / JWT）
- [ ] スクレイピング手動実行UI

---

## 📊 システム構成

### アーキテクチャ

```
┌─────────────────────────────────────────────────────────────────┐
│                         Docker Compose                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │ Next.js     │  │ Go Backend  │  │ Meilisearch │           │
│  │ Frontend    │→ │ (Gin)       │→ │ (検索)      │           │
│  │ :5176       │  │ :8084       │  │ :7700       │           │
│  └─────────────┘  └─────────────┘  └─────────────┘           │
│                          │                                      │
│                          ↓                                      │
│                   ┌─────────────┐                              │
│                   │ MySQL 8.x   │                              │
│                   │ (GORM)      │                              │
│                   │ :3308       │                              │
│                   └─────────────┘                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### データフロー

```
1. スクレイピング:
   Yahoo不動産 → Scraper → MySQL → Meilisearch

2. 検索:
   ユーザー → Frontend → Backend → Meilisearch → 結果

3. 差分検出:
   Scheduler → Scraper → MySQL → Snapshot → PropertyChange

4. 毎日更新:
   Cron → Scheduler → 全物件再取得 → 差分検出 → 論理削除
```

---

## 🚀 使い方

### 1. 起動

```bash
# Dockerコンテナを起動
docker-compose up -d

# ログ確認
docker logs realestate-backend --tail 100 -f
```

### 2. PoC検証

```bash
# ガイドを参照
cat POC-TEST-GUIDE.md

# 自動テスト実行
docker exec -it realestate-backend /bin/sh
cd /app
go run cmd/test-poc/main.go
```

### 3. スクレイピング実行

```bash
# 手動実行（管理者API）
curl -X POST http://localhost:8084/api/admin/scheduler/run

# 自動実行（スケジューラー）
# backend/config/scraper_config.yaml で設定:
#   daily_run_enabled: true
#   daily_run_time: "02:00"
```

### 4. 検索

```bash
# 全物件取得
curl http://localhost:8084/api/properties?limit=10

# キーワード検索
curl "http://localhost:8084/api/search?q=新宿"

# 高度な検索
curl -X POST http://localhost:8084/api/search/advanced \
  -H "Content-Type: application/json" \
  -d '{
    "query": "渋谷",
    "min_rent": 80000,
    "max_rent": 150000,
    "floor_plans": ["1K", "1DK"],
    "sort": "rent_asc",
    "limit": 20
  }'
```

### 5. フロントエンド

```bash
# ブラウザでアクセス
http://localhost:5176

# 機能:
# - 検索・フィルタリング
# - 候補・微妙・除外（localStorage永続化）
# - 非表示機能（ワンタップ）
# - メモ機能
```

---

## 🔒 安全運用のガイドライン

### BAN対策

1. **レート制限を厳守**
   - `request_delay_seconds: 2` 以上
   - `concurrent_limit: 1`（同時実行禁止）

2. **エラー時の対応**
   - `stop_on_error: true` 推奨
   - 403/429エラー → 1時間以上待機
   - 5xxエラー → exponential backoff

3. **1日の上限**
   - `max_requests_per_day: 5000` 推奨
   - 超過時は翌日まで停止

4. **User-Agent設定**
   - ブラウザを模倣した User-Agent
   - 固定値を使用

5. **実行タイミング**
   - 深夜〜早朝（2:00 AM推奨）
   - 1日1回のみ

### データ保護

1. **絶対にしてはいけないこと**
   - ❌ 既存データの物理削除
   - ❌ Dockerコンテナの削除
   - ❌ マイグレーションでのリフレッシュ
   - ❌ `DELETE` や `TRUNCATE` の実行

2. **論理削除を使用**
   - ✅ `status = 'removed'`
   - ✅ `removed_at` にタイムスタンプ
   - ✅ 検索からは除外

3. **バックアップ**
   - 定期的なDBバックアップ
   - Meilisearchのインデックスバックアップ

---

## 📈 監視・メンテナンス

### ログ確認

```bash
# バックエンドログ
docker logs realestate-backend --tail 100 -f

# MySQLログ
docker logs realestate-mysql --tail 100

# Meilisearchログ
docker logs realestate-meilisearch --tail 100
```

### ヘルスチェック

```bash
# バックエンド
curl http://localhost:8084/health

# Meilisearch
curl http://localhost:7700/health

# MySQL
docker exec -it realestate-mysql mysql -u realestate_user -p -e "SELECT COUNT(*) FROM properties;"
```

### 統計情報

```bash
# 物件数
curl http://localhost:8084/api/properties/count

# ステータス別集計
# TODO: 管理画面APIで実装予定
```

---

## 🐛 トラブルシューティング

### スクレイピングが失敗する

```bash
# ログを確認
docker logs realestate-backend --tail 100

# Circuit breakerの状態確認
# ログに "circuit breaker open" が出る場合は1時間待機

# レート制限の確認
# "rate limit exceeded" の場合は設定を見直す
```

### 検索結果が返らない

```bash
# Meilisearchの状態確認
curl http://localhost:7700/health

# インデックスの確認
curl -H "Authorization: Bearer masterKey123" \
  http://localhost:7700/indexes/properties/stats

# インデックスが空の場合、再スクレイピング
```

### フロントエンドで画像が表示されない

1. 開発者ツールでネットワークタブを確認
2. 画像URLに直接アクセスして確認
3. 404エラーの場合は画像URL失効の可能性

---

## 📚 関連ドキュメント

- **TODO.md**: 開発タスク一覧
- **POC-TEST-GUIDE.md**: PoC検証ガイド
- **docker-compose.yml**: Docker構成
- **backend/config/scraper_config.yaml**: スクレイパー設定

---

## 🎯 次のステップ

1. **PoC検証の実施**（POC-TEST-GUIDE.md参照）
2. 検証結果に基づく調整
3. Phase 4.5: 物理削除バッチの実装
4. Phase 6: 管理画面の実装

---

**最終更新**: 2025-12-17
**バージョン**: MVP Phase 0-3 Complete
